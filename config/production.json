{
    "database": {
      "host": "${DB_HOST}",
      "port": "${DB_PORT}",
      "name": "${DB_NAME}"
    },
    "googleCloud": {
      "credentials": "${GOOGLE_APPLICATION_CREDENTIALS}"
    }
  }
  